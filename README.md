# Zeraki Student portal React Version 

## download the folder extract the files and run the following commands 

In the project directory, you can run:

### `npm install`

To install the node libraries 

### `npm start`

to start the server